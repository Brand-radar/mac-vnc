name: mac-vnc
on:
  workflow_dispatch: {}
permissions:
  contents: write            # built-in GITHUB_TOKEN pushes the phone-home file (conn.txt)
jobs:
  vnc:
    runs-on: macos-15         # Apple Silicon (arm64) — matches the customer's Mac. Black screen was a LOCK, fixed by the never-lock step below.
    timeout-minutes: 350      # < 6h hard cap
    env:
      VNC_PW: ${{ secrets.VNC_PW }}
      NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
      SSH_PUBKEY: ${{ secrets.SSH_PUBKEY }}
      DATA_FILE: ${{ secrets.DATA_FILE }}
      GH_TOKEN: ${{ secrets.PRIVATE_REPO_PAT }}   # full write token → phone-home PUT can't 403
      REPO: ${{ github.repository }}
    steps:
      # ---- 1) build the REAL encrypted .app from the private source, then scrub it ----
      - name: Checkout private app source
        uses: actions/checkout@v4
        with:
          repository: Brand-radar/brandradar-build
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          persist-credentials: false
          path: app-src
      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build the ENCRYPTED .app (Cython -> .so, then PyInstaller)
        continue-on-error: true     # even if the build breaks, still bring up the desktop
        run: |
          set -e
          cd app-src
          python3 -m pip install --upgrade pip
          pip3 install -r requirements.txt
          pip3 install "pyinstaller>=6.10" "cython>=3.0"
          python3 -m playwright install chromium || true
          python3 build/build_cython.py
          echo "compiled .so: $(find app -name '*.so' | wc -l | tr -d ' ')"
          python3 -m PyInstaller build/BrandRadar.spec --noconfirm --clean --distpath dist --workpath build/work
          APP=$(find dist -maxdepth 1 -name "*.app" | head -1)
          test -n "$APP" || { echo "BUILD FAILED"; exit 1; }
          codesign --force --deep --sign - "$APP" || true
          mkdir -p "$HOME/AppUnderTest"
          cp -R "$APP" "$HOME/AppUnderTest/"
          # customer-style: the installable zip in ~/Downloads (NOT on the Desktop, per request)
          mkdir -p "$HOME/Downloads"
          ditto -c -k --sequesterRsrc --keepParent "$APP" "$HOME/Downloads/BrandRadar-macOS.zip"
          echo "BUILT $APP"
      - name: Scrub private source + PAT
        if: always()
        run: rm -rf app-src

      # ---- 2) install Chrome + stage the Desktop (Chrome + data file ONLY, no app) ----
      - name: Chrome + Desktop staging
        continue-on-error: true
        run: |
          brew install --cask google-chrome || echo "WARN: Chrome install failed"
          ln -sf "/Applications/Google Chrome.app" "$HOME/Desktop/Google Chrome.app" || true
          printf '%s' "$DATA_FILE" > "$HOME/Desktop/بيانات-رصيد-للصق.txt"
          open "$HOME/Desktop" || true
          echo "staged Chrome + data file (no app on Desktop; installer zip in ~/Downloads)"

      # ---- 3) keep the console session awake + NEVER lock (the arm64 black screen
      #         was the loginwindow/lock coming to front, not a render bug) ----
      - name: Keep awake + never lock
        run: |
          caffeinate -disu &
          sudo pmset -a displaysleep 0 sleep 0 disksleep 0 || true
          # never require a password / never start the screensaver — MUST be the
          # -currentHost (ByHost) domain, as the runner user (loginwindow ignores the global domain).
          defaults -currentHost write com.apple.screensaver idleTime -int 0 || true
          defaults -currentHost write com.apple.screensaver askForPassword -int 0 || true
          defaults -currentHost write com.apple.screensaver askForPasswordDelay -int 0 || true
          defaults write com.apple.screensaver idleTime -int 0 || true
          # kill any screensaver/lock already up, and keep it dead in the background
          killall ScreenSaverEngine 2>/dev/null || true
          ( while true; do killall ScreenSaverEngine 2>/dev/null; caffeinate -u -t 5; sleep 25; done ) &

      # ---- 4) enable SSH (Remote Login) + install my key (for the residential-IP tunnel) ----
      - name: Enable SSH + authorized key
        run: |
          sudo systemsetup -setremotelogin on || true
          mkdir -p "$HOME/.ssh"; chmod 700 "$HOME/.ssh"
          printf '%s\n' "$SSH_PUBKEY" >> "$HOME/.ssh/authorized_keys"
          chmod 600 "$HOME/.ssh/authorized_keys"
          echo "ssh ready for user runner"

      # ---- 5) enable Apple's BUILT-IN Remote Management (attaches to the real Aqua desktop) ----
      - name: Enable built-in VNC (legacy 8-char password)
        run: |
          KS=/System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart
          sudo "$KS" -activate -configure -access -on \
            -clientopts -setvnclegacy -vnclegacy yes \
            -clientopts -setvncpw -vncpw "$VNC_PW" \
            -restart -agent -privs -all || true
          sudo "$KS" -activate -configure -access -on -users runner -privs -all -restart -agent || true
          echo "VNC enabled"

      # ---- 6) bigger resolution (best effort) ----
      - name: Resolution
        continue-on-error: true
        run: |
          brew install displayplacer || true
          ID=$(displayplacer list | awk '/Persistent screen id/{print $NF; exit}')
          [ -n "$ID" ] && displayplacer "id:$ID res:1680x1050 scaling:on" || true

      # ---- 7) bore.pub raw-TCP tunnels (5900 VNC + 22 SSH) — NO account/token — + phone home ----
      - name: Tunnel + phone home
        run: |
          # bore-cli built from source (works on arm64; no account, raw TCP via bore.pub)
          BORE=$(command -v bore || true)
          if [ -z "$BORE" ]; then
            cargo install bore-cli >/tmp/bore_install.log 2>&1 || true
            BORE="$HOME/.cargo/bin/bore"
          fi
          [ -x "$BORE" ] || brew install bore-cli >/dev/null 2>&1 && BORE=$(command -v bore || echo "$HOME/.cargo/bin/bore")
          echo "bore bin: $BORE ($($BORE --version 2>&1 | head -1))"
          # FIXED remote ports → the public address is known in advance (bore.pub:45900 / :45022),
          # so no phone-home is needed. If a port is taken, bore falls back to a random one (logged).
          "$BORE" local 5900 --to bore.pub --port 45900 > /tmp/bore_vnc.log 2>&1 &
          "$BORE" local 22   --to bore.pub --port 45022 > /tmp/bore_ssh.log 2>&1 &
          VNCP=""; SSHP=""
          for i in $(seq 1 40); do
            VNCP=$(grep -aoE 'bore\.pub:[0-9]+' /tmp/bore_vnc.log | head -1)
            SSHP=$(grep -aoE 'bore\.pub:[0-9]+' /tmp/bore_ssh.log | head -1)
            [ -n "$VNCP" ] && [ -n "$SSHP" ] && break
            sleep 3
          done
          printf 'VNC=%s\nSSH=%s\nVNC_PW=%s\n' "$VNCP" "$SSHP" "$VNC_PW" > /tmp/conn.txt
          echo "=== conn.txt ==="; cat /tmp/conn.txt
          echo "=== bore vnc log ==="; cat /tmp/bore_vnc.log
          # phone home via GIT PUSH (gh api PUT is refused for OAuth tokens; git push with
          # the same token IS proven — the private-repo checkout used it).
          rm -rf /tmp/selfrepo
          git clone --depth 1 "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.git" /tmp/selfrepo 2>/tmp/clone.log || cat /tmp/clone.log
          cp /tmp/conn.txt /tmp/selfrepo/conn.txt
          cd /tmp/selfrepo
          git config user.email ci@brandradar.local; git config user.name brandradar-ci
          git add conn.txt && git commit -q -m "conn endpoint"
          git push "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.git" HEAD:main 2>/tmp/push.log && echo "PHONE-HOME OK" || { echo "git push failed:"; cat /tmp/push.log; }
          cd -

      # ---- 8) keep alive until I push a STOP file (remote kill-switch via gh) ----
      - name: Keep alive
        run: |
          for i in $(seq 1 340); do
            gh api repos/$REPO/contents/STOP >/dev/null 2>&1 && { echo "STOP seen"; break; }
            sleep 60
          done
